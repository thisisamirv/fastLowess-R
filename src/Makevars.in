TARGET_DIR = ../target
LIBDIR = $(TARGET_DIR)/release
STATLIB = $(LIBDIR)/libfastlowess.a

# Linking flags (substituted by configure)
PKG_LIBS = -L$(LIBDIR) @UNDEFINED_FLAGS@ -lfastlowess @GC_SECTIONS@

all: clean $(SHLIB)

$(SHLIB): $(STATLIB)

$(STATLIB):
	@echo "Reporting Rust compiler versions:"
	@cargo --version
	@rustc --version
	cargo build --release --manifest-path=Cargo.toml --target-dir $(TARGET_DIR) --config cargo/config.toml

clean:
	rm -Rf $(SHLIB) $(STATLIB) $(OBJECTS)

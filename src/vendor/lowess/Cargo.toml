# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85.0"
name = "lowess"
version = "0.7.0"
authors = ["Amir Valizadeh <thisisamirv@gmail.com>"]
build = false
include = [
    "src/**/*",
    "docs/**/*.svg",
    "Cargo.toml",
    "README.md",
    "LICENSE-MIT",
    "LICENSE-APACHE",
    "CHANGELOG.md",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "LOWESS (Locally Weighted Scatterplot Smoothing) implementation in Rust"
homepage = "https://github.com/thisisamirv/lowess"
documentation = "https://docs.rs/lowess"
readme = "README.md"
keywords = [
    "lowess",
    "smoothing",
    "statistics",
    "regression",
    "bioinformatics",
]
categories = [
    "science",
    "algorithms",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/thisisamirv/lowess"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[features]
default = ["std"]
dev = []
std = [
    "num-traits/std",
    "wide/std",
]

[lib]
name = "lowess"
path = "src/lib.rs"

[dependencies.num-traits]
version = "0.2.19"
features = ["libm"]
default-features = false

[dependencies.wide]
version = "1.1.1"
default-features = false

[dev-dependencies.approx]
version = "0.5.1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
